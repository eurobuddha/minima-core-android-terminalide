<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- allowBackup stays TRUE here on purpose — see res/xml/data_extraction_rules.xml. This app's command
         history (`terminalide.db`) is stored VERBATIM and can contain seed phrases and vault passwords, so it
         must never reach a cloud; but the user's own KISS scripts (`scripts.db`) have no export path, so a
         blanket disable would destroy real work on a new phone. The rules therefore exclude everything from
         cloud backup and carry scripts.db ONLY over same-user device transfer. -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Terminal"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ide.ScriptEditorActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Terminal session exports shared as attachments (oversized EXTRA_TEXT would
             blow the Binder transaction limit). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver android:name=".receiver.MinimaNotifyReceiver" android:exported="true">
            <intent-filter>
                <action android:name="org.minimarex.minimacore.NOTIFY" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
